package itheima.www.packages;

public class Package {
    //包就是文件夹，用来管理功能不同的java类，方便后期代码维护
    //公司域名反写+包的作用，需要全部英文小写，见名知意
    //包名加类名叫全类名，全限定名
    public static void main(String[] args) {
        //com.itheima.domain.Student s = new com.itheima.domain.Student();
        //但是太麻烦了，所以可以在上面导包import
        /*
        使用同一个包中的类时，不需要导包
        使用java.lang包中的类，不需要导包
        其他情况都需要导包
        如果同时使用两个包中的同类名，需要用全类名
         */
    }
}
